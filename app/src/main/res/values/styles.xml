<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color.primary</item>
        <item name="colorPrimaryDark">@color/color.primary.dark</item>
        <item name="colorAccent">@color/color.accent</item>
        <item name="colorControlNormal">@color/color.divider</item>
        <item name="android:textColorHint">@color/text.hint</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CardView.Title">
        <item name="android:paddingTop">@dimen/cardview.margin.slim</item>
        <item name="android:paddingBottom">@dimen/cardview.margin.slim</item>
        <item name="android:paddingLeft">@dimen/cardview.margin</item>
        <item name="android:paddingRight">@dimen/cardview.margin.slim</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="CardView.Chart">
        <item name="android:paddingTop">@dimen/cardview.margin.slim</item>
        <item name="android:paddingBottom">@dimen/cardview.margin.slim</item>
        <item name="android:paddingLeft">@dimen/cardview.margin</item>
        <item name="android:paddingRight">@dimen/cardview.margin</item>
    </style>

    <style name="AppTheme.EditText" parent="TextAppearance.AppCompat.Body1"/>

    <style name="AppTheme.Button" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorButtonNormal">@color/color.primary.dark</item>
        <item name="colorAccent">@color/color.accent</item>
    </style>

</resources>
